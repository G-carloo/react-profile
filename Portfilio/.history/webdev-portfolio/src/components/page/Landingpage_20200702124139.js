import React from "react";
import PropTypes from "prop-types";
import stars from "../../";

const Landingpage = (props) => {
  return <div>backgroundImage: `url(${stars})`</div>;
};

Landingpage.propTypes = {};

export default Landingpage;
