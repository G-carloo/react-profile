import React from "react";
import PropTypes from "prop-types";
import image from "stars";

const Landingpage = (props) => {
  return;
  <div>
    <img src='stars' alt='image' />
  </div>;
};

Landingpage.propTypes = {};

export default Landingpage;
